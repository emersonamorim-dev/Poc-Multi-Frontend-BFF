import React from 'react';
import '../styles/header.scss';

const Header: React.FC = () => {
    return (
        <header>
            <h3>Poc Aplicação React com Vite TS/Redux</h3>
        </header>
    );
};

export default Header;
